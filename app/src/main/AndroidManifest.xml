<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sacmakassar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_operator"></activity>
        <activity
            android:name=".SAC_Operator"
            android:label="@string/title_activity_sac__operator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SAC_Costumer"
            android:label="@string/title_activity_sac__costumer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".daftar_costumer" />
        <activity android:name=".lupa_password" />
        <activity android:name=".login_costumer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>